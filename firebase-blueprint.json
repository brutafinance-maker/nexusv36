{
  "entities": {
    "PBLPlaylist": {
      "title": "Playlist de Estudo",
      "description": "Uma pasta/coleção de documentos dentro de um módulo PBL.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "moduleId": { "type": "string" },
        "title": { "type": "string" },
        "color": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "moduleId", "title"]
    },
    "PBLDocument": {
      "title": "Documento Digital",
      "description": "Um documento individual tipo Notion dentro de uma playlist.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "playlistId": { "type": "string" },
        "title": { "type": "string" },
        "blocks": { "type": "array" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "playlistId", "title"]
    }
  },
  "firestore": {
    "/pblPlaylists/{playlistId}": {
      "schema": "PBLPlaylist",
      "description": "Coleção de playlists organizadas por módulo"
    },
    "/pblDocuments/{documentId}": {
      "schema": "PBLDocument",
      "description": "Coleção de documentos dentro das playlists"
    }
  }
}
